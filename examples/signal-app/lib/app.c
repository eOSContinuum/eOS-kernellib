/*
 * Signal-application library.
 *
 * Thin marker layer for the domain's singletons, mirroring the shape
 * of the other bundled examples (vault-app, merry-app, chat-app) so
 * application authors see one parallel pattern everywhere.
 *
 * Inheritors are static masters (one-of-a-kind daemons under sys/),
 * not clonables.
 */

static void create()
{
}
